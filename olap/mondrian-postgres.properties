# ============================================================================
# Mondrian OLAP Server Configuration - Ecommerce BI (PostgreSQL)
# ============================================================================

# ---------------------------
# JDBC CONNECTION (PostgreSQL)
# ---------------------------
mondrian.jdbcDrivers=org.postgresql.Driver
mondrian.jdbc=jdbc:postgresql://localhost:5432/ecommerce_bi
mondrian.jdbcUser=bi_user
mondrian.jdbcPassword=bi_password

# ---------------------------
# CATALOG / SCHEMA
# ---------------------------
mondrian.catalogURL=./olap/ecommerce_catalog.xml
mondrian.schema.validate=true
mondrian.xmla.drillthroughTotalCount.enable=true

# ---------------------------
# CACHING
# ---------------------------
mondrian.rolap.star.disableCaching=false
mondrian.expCache.enable=true
mondrian.rolap.SparseSegmentDensityThreshold=0.5

# ---------------------------
# QUERY EXECUTION
# ---------------------------
mondrian.query.limit=100000
mondrian.rolap.maxConstraints=1000
mondrian.result.highCardChunkSize=1000

# ---------------------------
# LOGGING
# ---------------------------
mondrian.rolap.generate.formatted.sql=true

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
# 1) Ensure PostgreSQL JDBC driver JAR is on the classpath.
# 2) Replace credentials above for your environment.
# 3) Restart Saiku/Pentaho server after any schema change.
